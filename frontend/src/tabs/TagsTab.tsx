import type { Organizer } from '../hooks/useOrganizer'

// Placeholder — implemented by the Tags audit area.
export default function TagsTab({ org }: { org: Organizer }) {
  void org
  return <div className="fl-empty">Tags audit — coming right up.</div>
}
