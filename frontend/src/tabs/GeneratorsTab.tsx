import type { Organizer } from '../hooks/useOrganizer'

// Placeholder — implemented by the Generators audit area.
export default function GeneratorsTab({ org }: { org: Organizer }) {
  void org
  return <div className="fl-empty">Generators audit — coming right up.</div>
}
