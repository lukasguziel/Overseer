{
  "_note": "Copy this file to 'config.json' (next to overseer.pyp). Structure/rules are normally maintained in the Rules tab or via presets; this is just an example. Old v1 configs (prefixes/groups) are migrated automatically.",

  "schema": 3,
  "casing": "PascalCase",
  "language": "en",
  "number_pad": 2,

  "translations": {
    "barhocker": "barstool"
  },

  "keeps": {
    "naming": [],
    "translate": [],
    "layers": [],
    "structure": [],
    "materials": []
  },

  "structure": [
    {
      "name": "Lights",
      "categories": ["light"],
      "aliases": ["lichter", "beleuchtung"],
      "priority": 100
    }
  ],

  "rules": [
    {
      "id": "prefix_lights",
      "type": "prefix",
      "prefix": "LGT_",
      "match": { "categories": ["light"] }
    },
    {
      "id": "renumber_meshes",
      "type": "renumber",
      "pad": 2,
      "per_parent": true,
      "match": { "categories": ["mesh"] }
    }
  ]
}
