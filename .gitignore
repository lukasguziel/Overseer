# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Analysis output
scene_report.json

# Node / Vite
frontend/node_modules/
frontend/dist/

# Vite build output (built from frontend/, distributed by deploy.ps1)
src/web/

# Vendored third-party wheels (Pillow), unpacked by tools/vendor_pillow.py and
# by the release workflow. Generated, never committed — no binaries in git.
src/vendor/

# C4D SDK reference archives (large vendor zips). Only the Python 2024 SDK
# relevant to this Python plugin is versioned; the rest stays local
# (C++ 2024-2026, ProjectTool exceed GitHub's 100 MB limit).
c4d_sdk/*
!c4d_sdk/Cinema_4D_Python_SDK_2024_5_0.zip

# IDE / temp
.idea/
resp.json
.coverage


# Deploy-Ziel: maschinenlokal, kommt NIE ins Repo (deploy-Skill schreibt sie)
.claude/skills/deploy/deploy.config.json

# runtime logs written by the plugin
change_history.json
analysis_history.json
scene_structure.csv
