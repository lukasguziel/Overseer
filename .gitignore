# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Analysis output
scene_report.json

# Node / Vite
frontend/node_modules/
frontend/dist/

# Local packaging output (release zips are built by the release workflow)
dist/

# Vite build output (built from frontend/, distributed by deploy.ps1)
src/web/

# Vendored third-party wheels (Pillow), unpacked by tools/vendor_pillow.py and
# by the release workflow. Generated, never committed — no binaries in git.
src/vendor/

# C4D SDK reference archives (large vendor zips) stay local, never in git
var/sdk/

# IDE / temp
.idea/
resp.json
.coverage


# Deploy-Ziel: maschinenlokal, kommt NIE ins Repo (deploy-Skill schreibt sie)
.claude/skills/deploy/deploy.config.json

# runtime logs written by the plugin
change_history.json
analysis_history.json
scene_structure.csv

# Playwright MCP session artifacts
.playwright-mcp/
